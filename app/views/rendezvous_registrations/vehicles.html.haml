.container
  .row
    = render :partial => 'common/registration_header', :locals => { :step => 'vehicles' }
  .row
    .col-md-offset-2.col-md-8.col-xs-12
      %p 
        %strong Congratulations, you have completed your registration for the Rendezvous!
      %p 
        It's helps us plan if we know what show vehicles you will be bringing. If the information below is correct, you may go directly to 
        = succeed '.' do
          = link_to 'your registration information', rendezvous_registration_path(@rendezvous_registration)
        Otherwise, please enter the correct information below.
      = simple_form_for @user, :id => 'rendezvous_registration_vehicles' do |u|      
        = render :partial => 'common/user_info', :locals => { :f => u, :vehicles => true }
        %br
        = u.button :submit, 'Save Vehicle(s)', :class => 'btn btn-primary hidden', :id => 'save-vehicle'
        = link_to 'View My Registration', rendezvous_registration_path(@user.rendezvous_registrations.last), :class => 'btn btn-info', :id => 'view-registration'
