(function($) {
  jQuery(document).ready(function() {

    // Set up sources and targets
    let $purchase = $('#purchase')
    let $cartItems = $('#cart-items')
    let $cartSubTotal = $('#subtotal')
    let $genericAmount = $('#commerce_purchase_amount')
    let $total = $('#total')

    // Set up event listeners
    
  })
})
