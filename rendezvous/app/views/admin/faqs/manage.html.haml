.container#faqs
  .row.justify-content-center
    .col-md-10
      %h1 Manage FAQs
    .col-md-8
      %h2 Ordering
      %table.table
        %tbody.sortable
          - @faqs.each_with_sortable_id do |faq, sortable_id|
            %tr{id: sortable_id}
              %td
                %svg.bi{ width: "32", height: "32", fill: "currentColor" }
                  %use{ href: image_path("bootstrap-icons/bootstrap-icons.svg#list") }
                = faq.name 
    .col-md-10
      %h2 Editing
      - @faqs.each do |faq|
        .card 
          .card-body 
            .card-text
              = simple_form_for faq do |f|
                = render partial:  'admin_faq_fields', locals: { f: f }
                %br
                .form-group
                  = f.button :submit, input_html:  { value:  'Update Content' }, class:  "btn btn-sm btn-primary"

      %p
        =link_to "Create new", new_admin_faq_path, class: "btn btn-sm btn-primary"
      %p
        =link_to "Import from CSV", admin_faqs_import_path, class: "btn btn-sm btn-warning"
      - if @faqs.size > 0
        %p
          =link_to "Destroy all", admin_destroy_all_faqs_path, method: :delete, class: "btn btn-sm btn-danger"  

:javascript
  $(function() {
    $('.sortable').railsSortable();
  });
    


              