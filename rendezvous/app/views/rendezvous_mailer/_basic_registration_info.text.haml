"- Number of people: #{@registration.attendees.size}"
"- Number of people for the Friday lake cruise: #{@registration.lake_cruise_number}"
"- Total registration amount: #{number_to_currency(@registration.total)}"

- if @registration.donation.positive?
    "- Donation (thank you!): #{number_to_currency(@registration.donation)}"
  %li Registration status: #{@registration.status.humanize}

Link to view your registration:
= link_to event_registration_url(@registration.id), event_registration_url(@registration.id)
