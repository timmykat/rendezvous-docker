.container
  .row
    .col-12
      .card
        .card-body
          .card-title
            %h1.h3 People's Choice Ballots
          .card-text
            %table.table
              %thead
                %tr
                  %th Type
                  %th Year
                  %th ID
                  %th Selections
              %tbody
                - @ballots.each do |b|
                  %tr
                    %td #{b.ballot_type}
                    %td #{b.year}
                    %td #{b.id}
                    %td
                      %ul
                        - b.selections.each do |v|
                          %li #{v.year_marque_model}
