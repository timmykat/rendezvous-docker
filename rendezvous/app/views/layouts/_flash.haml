- unless flash.keys.blank?
  .flash-wrapper
    - flash.keys.each do |type|
      - flash[type] = [ flash[type] ] if !flash[type].kind_of? Array
      - flash[type].each do |m|
        .flash{ role: type.to_s }
          .close-flash
            = icon(:close, size: '24')
          .message
            - if m.is_a?(Array)
              - m.each do |m1|
                = m1.html_safe unless m1.blank?
            - else
              = m.html_safe unless m.blank?
                
            




