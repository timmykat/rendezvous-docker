# config/initializers/logger_formatter.rb

# require Rails.root.join("config/formatters/custom_log_formatter")

# Rails.logger.formatter = CustomLogFormatter.new

# # Optional: if you're using tagged logging (most apps are), preserve it like this:
# if Rails.logger.respond_to?(:formatter=)
#   Rails.logger = ActiveSupport::TaggedLogging.new(Rails.logger)
# end